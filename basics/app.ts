console.log('Hello, world');

let num;  // not need initializing immediately
num = 10;  // we can change the value of num

let Num = 10;  // case insensitive

const str = 'Hello, world';  // initializing immediately  , we can't change the value of strf